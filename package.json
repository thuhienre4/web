{
  "name": "alocoupon",
  "version": "1.0.0",
  "description": "AloCoupon coupon marketplace with a small Node.js admin API.",
  "main": "local-server.js",
  "scripts": {
    "start": "node local-server.js",
    "dev": "set OPEN_BROWSER=1&& node local-server.js",
    "check": "node --check local-server.js && node --check script.js && node --check scripts/extract-brand-logos.js && node --check scripts/extract-brand-landings.js && node --check scripts/extract-product-images.js && node --check scripts/sync-trustpilot-reviews.js",
    "extract:logos": "node scripts/extract-brand-logos.js",
    "extract:landings": "node scripts/extract-brand-landings.js",
    "extract:products": "node scripts/extract-product-images.js",
    "sync:trustpilot": "node scripts/sync-trustpilot-reviews.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "private": true
}
